To make the codes run, open your terminal (command propmt) and enter these commands:

cd env\Scripts
activate
cd ../..

You can now run the codes by clicking the run button on your PyCharm IDE.

You can also run the code via terminal, to do so, enter the command below:
python <yourPythonScript.py>

Instructions are provided via comments